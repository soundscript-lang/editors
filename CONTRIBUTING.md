# Contributing

Use pull requests against `main` for public editor and extension changes.

Before opening a pull request:

- run `npm install`
- run `npm test`
- run `npm run package:vscode`

If the change affects extension publishing or marketplace behavior, include the
intended install and upgrade path in the pull request description.
