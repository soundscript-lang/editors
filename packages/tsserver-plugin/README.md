# @soundscript/tsserver-plugin

TypeScript server plugin support for soundscript editor integrations.

This package is consumed by the VS Code extension in the same repo and is intended for editor/runtime integrations that project `.sts` files through TypeScript.
